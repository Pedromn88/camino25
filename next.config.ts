import type { NextConfig } from "next";


const nextConfig: NextConfig = {
    sassOptions: {
        // Apunta a la ubicación donde tienes tus estilos
        includePaths: ['./styles'],
        // Puedes añadir más opciones según necesites
      },


      
    
};



export default nextConfig;
